<font color="green"> <b>Total Students applied for this course </b></font> <%=@course_students.count%> 
 
 <div class="table-responsive">
  Download Report:
          <%#= link_to "CSV", student_s_home_url(format: "csv") %> 
          <%= link_to "Excel File", export_course_report_reports_path(format: "xlsx" ,course_id: params[:course_id])  , class: "btn btn-success",id: "idExcel" %>
        </p>
          <table class="table table-striped table-bordered table-hover dataTables-example" >
            <thead>
              <tr>
              <th>Student Name</th>
              <th>University Name</th>
              <th>Course</th>
              <th>Enrollment No</th>
              <th></th>
             </tr>
           </thead>
           <tbody>
      
          <%unless @course_students.nil?%>
            <% @course_students.each do |b|%>
            <tr class="gradeX">
            <td><%= b.first_name+"-"+b.last_name%></td>
            <td><%= b.university.university_name%></td>
            <td><%= b.course.course_name %></td>
            <td><%= b.enrollment %></td>
            <td><%= link_to 'View Profile', student_path(b), class: 'btn btn-link'%></td>
            </tr>
           <%end%>
           <%end%>
          </tbody>
        </table>
      </div>

      